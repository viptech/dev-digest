import { ProjectContextPage } from "./_components/ProjectContextPage";

/* Route: /repos/:repoId/context (SPEC-01, Project Context). Thin route entry. */
export default function ContextPage() {
  return <ProjectContextPage />;
}
