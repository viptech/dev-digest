"use client";

/* TEMPORARY stub — replaced by the real preview/import drawer in Task 9
   (same file paths). Intentional per the Skills-core plan, not a shortcut. */
export function SkillDrawer({ onClose }: { mode: string; skillId?: string; onClose: () => void }) {
  return null;
}
