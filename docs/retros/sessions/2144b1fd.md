## Ретроспектива — 2144b1fd

**Обсяг**: уся сесія, 19 агентів (main + 18 subagents, макс. глибина d1), 11г18хв wall-clock (20:25 21.08 → 07:44 22.08 UTC)
**Вартість**: 42.1M зважених токенів (341.0M сирих, 98.4% кешованих читань)

Сесія охоплює лабораторну L07: читання завдання, дизайн-рев'ю в браузері, дві спеки (`spec-creator`), два Development Plan (`implementation-planner`), два git worktree з 4+5 implementer-групами, по одному `architecture-reviewer`+`plan-verifier` на кожну спеку, git-хірургію мержу (rebase + розв'язання конфліктів) і три реальні GitHub PR.

### Дії

1. **Два subagent-дизпатчі впали через ліміт сесії/API і потребували ручного `/login` користувача для відновлення** (`implementer/a9b23da9` — SPEC-08 Group 4, `plan-verifier/ad142a44` — SPEC-07, обидва позначені `<synthetic>` у моделі — ознака компакції всередині транскрипту). Обидва — найважчі завдання свого класу (Group 4 охопила п'ять кроків wizard'а й 22 нові файли; повторний `plan-verifier` виконував вичерпну звірку 31 AC+16 задач читанням коду). Для планів такого розміру варто або дробити "важкі" групи ще на 2, або закладати в план явний чекпоінт "звірити прогрес on session resume", а не покладатись на те, що одна сесія витягне все за раз.
2. **79% tool-виводів (`main`) — це самотні виклики (`batch: 1` = 1105 із 1393)**, попри що велика частина git-хірургії мержу дійсно послідовна (rebase-конфлікт → resolve → continue не паралелиться). Але частина верифікаційних прогонів (typecheck+unit+it.test у двох worktree) виконувались послідовно там, де могли б піти одним батчем — наступного разу batch-ити незалежні `Bash`-виклики (typecheck server + typecheck client одного worktree, або обидва worktree одночасно), а не пускати їх один за одним.
3. **`main` тримає 54.6% усього зваженого бюджету сесії** (23.0M із 42.1M) — очікувано для цього типу задачі (координатор сам робив дизайн-рев'ю в браузері, git rebase/conflict-резолюцію, прямі коду-фікси поза subagent-петлею), але вартий явного визнання: делегування control-plane роботи (git-хірургія, фінальна верифікація) subagent'у тут не мало сенсу (потребує живого контексту всієї сесії), а от частину прямих фіксів (кластеризація, wire-контракт) можна було б віддати короткому fixer-subagent'у в межах bounded fix-loop, який план і так передбачав — цього бюджету (2 субагенти на фікс-луп) урешті не торкнулись, бо координатор фіксив усе сам.

### Агенти

| Агент | Модель | Тривалість | Скіли | Токени (зважені / сирі) |
|---|---|---|---|---|
| main | claude-sonnet-5 | 2г43хв (act.) | workflow-retro | 23.0M / 197.2M |
| implementer/a9b23da9 (SPEC-08 Group 4, ретрай після ліміту) | claude-sonnet-5 | 18м36с | engineering-insights, pr-self-review, react-best-practices, react-ui-architecture | 2.61M / 19.4M |
| implementer/a3b5b6fa (SPEC-08 Group 3) | claude-sonnet-5 | 19м19с | drizzle-orm-patterns, engineering-insights, onion-architecture | 2.58M / 21.8M |
| implementer/af2db994 (SPEC-07 Group 4) | claude-sonnet-5 | 15м49с | engineering-insights, pr-self-review | 1.88M / 15.6M |
| implementer/a4d60b23 (SPEC-07 Group 3) | claude-sonnet-5 | 15м13с | engineering-insights, pr-self-review | 1.86M / 15.6M |
| implementer/aa9a718b (SPEC-08 Group 2) | claude-sonnet-5 | 14м39с | engineering-insights, onion-architecture | 1.49M / 11.1M |
| implementer/a0aa13c1 (SPEC-08 Group 5) | claude-sonnet-5 | 10м37с | engineering-insights | 1.45M / 11.9M |
| spec-creator/ac8b0e7c (SPEC-07) | claude-sonnet-5 | 13м52с | security | 1.32M / 9.6M |
| spec-creator/a4d5669c (SPEC-08) | claude-sonnet-5 | 15м30с | security | 1.07M / 7.9M |
| implementer/a7bfb263 (SPEC-07 Group 2) | claude-sonnet-5 | 7м37с | engineering-insights, fastify-best-practices, onion-architecture, security, zod | 0.80M / 5.9M |
| implementer/aadcf1ed (SPEC-08 Group 1) | claude-sonnet-5 | 5м51с | drizzle-orm-patterns, engineering-insights, onion-architecture, postgresql-table-design, zod | 0.66M / 4.6M |
| plan-verifier/a4c7887f (SPEC-08) | claude-sonnet-5 | 5м24с | — | 0.58M / 3.5M |
| plan-verifier/a1bca105 (SPEC-07) | claude-sonnet-5 | 7м13с | — | 0.50M / 3.4M |
| implementation-planner/a6da77d0 (SPEC-07) | claude-sonnet-5 | 6м09с | — | 0.46M / 2.5M |
| plan-verifier/ad142a44 (SPEC-07, 1-ша спроба, впала на ліміті) | claude-sonnet-5 | 6м31с | — | 0.43M / 3.0M |
| implementer/a7fe53a4 (SPEC-07 Group 1) | claude-sonnet-5 | 4м07с | drizzle-orm-patterns, engineering-insights, zod | 0.39M / 2.5M |
| architecture-reviewer/a7eec7ba (SPEC-07) | claude-sonnet-5 | 5м08с | — | 0.38M / 2.1M |
| architecture-reviewer/ac96e51d (SPEC-08) | claude-sonnet-5 | 3м14с | — | 0.36M / 2.2M |
| implementation-planner/afefb712 (SPEC-08) | claude-sonnet-5 | 5м41с | — | 0.27M / 1.2M |

Жоден агент не викликав `Skill` понад список у таблиці; `plan-verifier`/`architecture-reviewer`/`implementation-planner` — «—» (жодного `Skill`-виклику, очікувано для read-only ролей).

### Куди пішла вартість

1. **`main` (23.0M, 54.6%)** — координатор: дизайн-рев'ю в браузері (Chrome MCP), git worktree/rebase-хірургія (3 конфлікт-раунди), пряма верифікація (typecheck/unit/integration ×3 worktree ×кілька разів), і прямі фікси (кластеризація, wire-контракт, AC-12/AC-20, RunnerBundleReader-порт) — свідоме рішення координатора фіксити самому, не через fix-loop subagent.
2. **implementer/a9b23da9 + a3b5b6fa (5.19M, 12.3%)** — SPEC-08 Groups 3+4 (GitHub API/ingest + Export Wizard), найбільші implementer-групи за обсягом нових файлів (Group 4 — 22 файли wizard'а).
3. **implementer/af2db994 + a4d60b23 (3.74M, 8.9%)** — SPEC-07 Groups 3+4 (клієнтський UI: Configure run, Columns/Tabs/Disagree).
4. **Обидва spec-creator (2.38M, 5.7%)** — глибоке дослідження кодової бази проти буквального тексту лабораторної (обидва знайшли суттєві розбіжності: відсутня конкурентність, відсутня group-евристика, PULL vs push ingest).

### Примітки

- **Паралелізм реально спрацював**: 9 пар агентів з `overlaps` — найбільша `implementer/a3b5b6fa` × `implementer/af2db994` (825с ≈ 13.75хв одночасно), підтверджує, що worktree A/B справді виконувались конкурентно на рівні кожної implementer-групи, а не лише номінально.
- **`duplicate_reads` не сигналізує проблему** — максимум 3 повторні читання, переважно `.claude/plans/*.md`/`INSIGHTS.md`/спек-файли — це очікуване, навмисне повторне читання плану кожною новою implementer-групою (multi-agent handoff за дизайном), не випадкова неефективність.
- **`main`'s `idle_s` = 30948с (8.6г) із 40694с повної тривалості транскрипту** — сесія тягнеться через нічну перерву й ~5 реальних розривів (включно з session-limit outage); `active_duration_s` (9760с ≈ 2.7г) — коректніша міра реальної роботи координатора, вона й використана в таблиці/підсумку вище.
- **Недооцінка in-context**: не рахувалась — цей звіт від початку написаний у deep-режимі.
- **0 конфліктів у продуктовому коді** при мержі (server modules/client components) — межі worktree A/B, зафіксовані в спеках заздалегідь, реально дотримались. Усі 6 конфліктних файлів при rebase — бухгалтерія (нумерація міграції, append-only INSIGHTS.md записи), не логіка.
